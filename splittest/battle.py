def battle(x,y):
    if x > y:
        #pop y from controller's field
        #Append to controller's discard pile
    elif x < y:
        #pop x from controller's field
        #append x to controller's discard pile
    else:
        #both effects from above
